# Execution Todo - 2026-04-04
Done: 20 / 40 | Pending: 20

- [ ] 添加 `.env.example` 文件，列出所有需要的环境变量及说明
- [ ] 确保 `.env` 被 `.gitignore` 排除
- [ ] 删除旧的单体脚本文件（重构完成后）
- [ ] 为 `parser.py` 添加健壮的 JSON 解析（使用 `json.JSONDecoder.raw_decode()` 替代手写括号匹配）
- [ ] 为 API 调用添加重试逻辑（指数退避，最多 3 次）
- [ ] 为 LLM 输出添加 schema 验证（检查字段类型、坐标范围、必填项）
- [ ] 添加设备坐标碰撞检测（验证布局中设备不重叠、满足间距要求）
- [ ] 为 `main()` 函数添加适当的错误处理和日志输出（使用 `logging` 模块替代 `print`）
- [ ] 为碰撞检测编写单元测试（重叠、边缘、正常间距）
- [ ] 为 API 调用编写 mock 测试（使用 `unittest.mock` 模拟 LLM 响应）
- [ ] 重写 `README.md`：项目简介、安装步骤、使用示例、架构说明、配置说明
- [ ] 添加 `ARCHITECTURE.md`：模块关系图、数据流、设计决策
- [ ] 为所有公共函数添加 docstring（Google 或 NumPy 风格）
- [ ] 更新 `large_equipment.json`：确保数据结构清晰、字段完整、注释充分
- [ ] 清理历史输出文件（`crude_salt_purification_layout_*.json`、`crude_salt_purification_room_design_*.txt`）
- [ ] `python -m pytest tests/ -q` 全部通过
- [ ] `python -m chemistry_lab --dry-run` 正常运行
- [ ] 完整流程测试：设备生成 → 布局生成 → 图像输出（需 API Key）
- [ ] 代码风格检查：`ruff check src/`（或 `flake8`）
- [ ] 确认无硬编码密钥：`grep -r "sk-" src/` 无结果
